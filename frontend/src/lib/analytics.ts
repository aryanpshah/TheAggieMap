export function search_submitted(query: string) {
  // TODO: Wire up analytics event dispatch.
  void query;
}

export function filter_toggled(filter: string, selected: boolean) {
  // TODO: Wire up analytics event dispatch.
  void filter;
  void selected;
}

export function card_clicked(id: string) {
  // TODO: Wire up analytics event dispatch.
  void id;
}
